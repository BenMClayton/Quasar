#[derive(Default, Debug)]
pub struct Inventory {
    pub wood: u32,
    pub stone: u32,
    pub copper_ore: u32,
    pub coal: u32,
    pub copper_plate: u32,
    pub copper_gear: u32,
    pub crystal: u32,
}
